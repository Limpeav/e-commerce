import React from 'react';
import { Loader2 } from 'lucide-react';
import { createSpinnerComponent } from "../../../../../shared-frontend/components/Spinner.jsx";

const Spinner = createSpinnerComponent({ React, Loader2 });

export default Spinner;
